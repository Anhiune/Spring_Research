# requirements for clean_sentiment_data.py
# Core
pandas>=2.0
ftfy>=6.1
emoji>=2.8
unidecode>=1.3

# Language detection (install at least one)
langdetect>=1.0.9
# fasttext>=0.9.2        # optional – faster & more accurate; needs lid.176.bin model

# Translation backends (install what you need)
# openai>=1.0            # --translate-backend openai
# google-cloud-translate # --translate-backend gcp
# transformers>=4.30     # --translate-backend hf
# sentencepiece          # needed by MarianMT models
# torch                  # needed by transformers

# Classic NLP (only for --model-type classic)
nltk>=3.8

# Near-duplicate detection (optional)
# datasketch>=1.6        # --near-dupes

# Testing
pytest>=7.0
